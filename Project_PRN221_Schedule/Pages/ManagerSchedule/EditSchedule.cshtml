@page
@model EditScheduleModel

@{
    ViewData["Title"] = "EditSchedule";
}

<h1>EditSchedule</h1>

<h4>WeekSchedule</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="WeekSchedule.Id" />
            <div class="form-group">
                <label asp-for="WeekSchedule.ScheduleId" class="control-label"></label>
                <select asp-for="WeekSchedule.ScheduleId" class="form-control" asp-items="ViewBag.ScheduleId"></select>
                <span asp-validation-for="WeekSchedule.ScheduleId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WeekSchedule.WeekIndex" class="control-label"></label>
                <input asp-for="WeekSchedule.WeekIndex" class="form-control" />
                <span asp-validation-for="WeekSchedule.WeekIndex" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WeekSchedule.RoomId" class="control-label"></label>
                <select asp-for="WeekSchedule.RoomId" class="form-control" asp-items="ViewBag.RoomId"></select>
                <span asp-validation-for="WeekSchedule.RoomId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WeekSchedule.GroupId" class="control-label"></label>
                <select asp-for="WeekSchedule.GroupId" class="form-control" asp-items="ViewBag.GroupId"></select>
                <span asp-validation-for="WeekSchedule.GroupId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WeekSchedule.SlotId" class="control-label"></label>
                <select asp-for="WeekSchedule.SlotId" class="form-control" asp-items="ViewBag.SlotId"></select>
                <span asp-validation-for="WeekSchedule.SlotId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
